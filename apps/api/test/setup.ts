// =============================================================================
// test/setup.ts — Global setup per Vitest in apps/api
// =============================================================================
// Placeholder per future global mocks/fixtures (env, dates, ecc.).
// Vitest carica questo file una volta prima di qualsiasi test file.
// =============================================================================

// Nessuna global mock attualmente necessaria. I test mock vi.fn() i providers
// NestJS individuali via Test.createTestingModule().
