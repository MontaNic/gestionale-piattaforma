# Carica nvm/PATH per ambienti non-interactive (GUI git client, IDE source
# control, script CI). Hook self-contained — vedi ADR-0004.
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

pnpm exec lint-staged
